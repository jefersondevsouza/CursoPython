from math import radians
from math import radians, sin, cos, tan
ang = float(input('Digite um algulo qualquer: '))
rad = radians(ang)
print('Para o algulo digitado, o sen é {}, o cos é {} e tang é {}'.format(sin(rad), cos(rad), tan(rad)))
